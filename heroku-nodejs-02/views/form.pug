doctype html
html
    head 
        meta(charset='utf-8')
    body
        H1 Hello FORM for POST testing
        P
        form(action='/form_receiver', method='post')
            P
                //- no space allows between input and '('
                input(type='text', name='title')
            P
                textarea(name='description')
            P
                input(type='submit')

        

        


